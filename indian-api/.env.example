# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration (comma-separated list of allowed origins)
# Use * for all origins or specify: https://yoursite.com,https://anothersite.com
ALLOWED_ORIGINS=*

# Database Configuration
# Choose database type: postgresql or sqlite
DB_TYPE=postgresql

# PostgreSQL Database (Required if DB_TYPE=postgresql)
# Get your free PostgreSQL database from: https://neon.tech or https://supabase.com
DATABASE_URL=postgresql://username:password@host:5432/database_name

# SQLite Database (Optional - only if DB_TYPE=sqlite)
# SQLITE_PATH=./recipe.sqlite
